node_modules
.next
.DS_Store
npm-debug.log*
yarn-error*
pnpm-debug*
.env*

# local backups
.checkpoints/
*.tar.gz

# local backups
.checkpoints/
*.tar.gz

# local backups — never commit
.checkpoints/
*.tar.gz

# local backups — never commit
.checkpoints/
*.tar.gz
